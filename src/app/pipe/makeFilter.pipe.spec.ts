import { MakeFilterPipe } from './makeFilter.pipe';

describe('MakefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
